apiVersion: v1
kind: Service
metadata:
  name: {{ include "ugoite.frontendServiceName" . }}
  labels:
    {{- include "ugoite.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    {{- include "ugoite.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
