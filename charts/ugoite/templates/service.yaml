apiVersion: v1
kind: Service
metadata:
  name: {{ include "ugoite.serviceName" . }}
  labels:
    {{- include "ugoite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ugoite.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
