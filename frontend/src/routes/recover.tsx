export default function RecoverRoute() {
  return (
    <main class="publicShell">
      <section class="publicCard ui-stack">
        <h1 class="ui-page-title">Passkey recovery is not available</h1>
        <p class="ui-muted">
          Ugoite v0.1 supports passwordless Passkey login and bootstrap-only
          recovery-code display. Account recovery and Passkey replacement are
          future functionality.
        </p>
      </section>
    </main>
  );
}
