// REQ-FE-010: Editor content persistence during save
import { describe, it, expect } from "vitest";

describe("legacy entry store tests", () => {
	it("legacy placeholder delegates to entry-store tests", () => {
		expect(true).toBe(true);
	});
});
