---
title: "v0.3 preflight: authority and surface evidence"
---

This page records the v0.3 preflight boundary. It is a navigation and decision
record, not a new product specification or release completion claim. The
generated report is a static projection of existing authorities; Mitase checks
declared relationships and evidence but does not run Ugoite tests or certify
product intent.

## Reproduce the projection

Run from the repository root:

```sh
mise exec -- deno run -A tools/capability_report.ts --scope v0.3-preflight --json
mise exec -- deno run -A tools/capability_report.ts --scope v0.3-preflight --markdown
```

The existing Golden Journey scope is unchanged:

```sh
mise exec -- deno run -A tools/capability_report.ts --json
mise exec -- deno run -A tools/capability_report.ts --markdown
```

The investigation plan identifies `74777bbdc8a609277d6adf87caaed3fcd2f669ef`
as its source snapshot. The PRE-01 implementation started from `origin/main`
at `bf1f99bab96d34bc423c2918b4ee7b8950f56e46`; these are separate facts. The
projection reads the checked-out source and must be regenerated for the commit
being reviewed. Its `source-located` status does not mean a test was run. Only
an independently recorded run tied to a command, source SHA, and result URL can
support `executed-and-passed`; this tool never emits that status.

## Read the report

The inventory contains one row for each preflight capability, with operations
or Host/resource pointers, Requirement/Criterion references, Feature Binding
references, source paths, surface observations, verification claims, test
selectors, availability, evidence status, reason, and follow-up issue. JSON and
Markdown are rendered from the same report object. Missing references or
artifacts remain visible as unresolved or missing evidence.

`availability` and `evidence_status` are independent. Availability describes
whether the capability exists or needs surface work. Evidence status describes
whether a declaration or source was located, whether evidence is missing, or
whether execution was not recorded. In particular, `existing` does not mean
every UI flow is complete, and a source path does not prove its behavior.
Each surface observation names its own source paths, so CLI core and CLI remote
do not inherit the same undifferentiated evidence list. These paths remain
static locators, not runtime reachability or authorization proofs.

The portable TypeScript and Rust manifests must match in content and order. That
static equality is distinct from operation reachability, authorization, MCP
Host behavior, WASM behavior, and E2E results. The read-only `ugoite://form/{id}`
resource is not a Form save operation. `ugoite.save` and `ugoite.delete` are MCP
facade tools, not invented portable REST operations. Remote `space.create`
remains intentionally outside the CLI path because setup requires the recent
Passkey and node-admin Browser ceremony documented in C3.

## Unresolved work and owners

| Gap or follow-up | Current evidence and expected outcome | Owner |
| --- | --- | --- |
| EntryQuery and SQL read lifecycle | Page, continuation, count, cancellation, stale-response handling, retry, and lazy count need separately observable behavior. Do not claim cancellation from page reachability. | [#3120](https://github.com/ugoite/ugoite/issues/3120) |
| SQL multi-page export | `sql.query` and `sql.query.count` are stateless operations. The CLI has no bounded multi-page NDJSON export path; one complete export must consume continuations and must not appear complete after an error. | [#3130](https://github.com/ugoite/ugoite/issues/3130) |
| Konase approval Host | Rust confirmation types or a CLI implementation do not establish Browser approval. Approval must occur before dispatch, remain tied to the active Space and request, and require a validated receipt for a successful save outcome. | [#3124](https://github.com/ugoite/ugoite/issues/3124) |
| C7 acceptance gate | C7 was planned for v0.2 but remained open after v0.2.0 shipped. Preflight reports gaps but does not turn them into release blockers or choose the acceptance policy. | [#2563](https://github.com/ugoite/ugoite/issues/2563); horizon clarification: [#3123](https://github.com/ugoite/ugoite/issues/3123) |
| Application Definition design | Design starts only after the read lifecycle and Host approval gates are met and unexplained semantic or authorization gaps have owners and exit conditions. | [#3131](https://github.com/ugoite/ugoite/issues/3131) |

The CLI one-shot approval work was delivered by [#3126](https://github.com/ugoite/ugoite/pull/3126), closing [#3125](https://github.com/ugoite/ugoite/issues/3125). That is implementation evidence for the CLI slice, not evidence that the Browser Host work in #3124 is complete.

## v0.2 Product UX carry-over

The eight `done: false` tasks in `docs/version/v0.2/product-ux.yaml` remain
unverified; their completion conditions are not reduced here and the tracker is
not edited to imply completion. They are:

| Tracker task | What still needs evidence | Follow-up |
| --- | --- | --- |
| Define outcomes for Entry, Form, Asset, search, and SQL journeys | Agreed observable completion outcomes and acceptance evidence for each journey. | [#3132](https://github.com/ugoite/ugoite/issues/3132), [#3120](https://github.com/ugoite/ugoite/issues/3120), [#3130](https://github.com/ugoite/ugoite/issues/3130) |
| Define discoverability and cross-surface consistency | A user-visible contract for Browser, CLI core, and CLI remote entry points. | [#3132](https://github.com/ugoite/ugoite/issues/3132), [#3120](https://github.com/ugoite/ugoite/issues/3120), [#3130](https://github.com/ugoite/ugoite/issues/3130) |
| Define validation clarity, recovery completion, and documentation correctness | Acceptance outcomes and evidence for those workflows, not just a shipped feature list. | [#3132](https://github.com/ugoite/ugoite/issues/3132), [#3124](https://github.com/ugoite/ugoite/issues/3124) |
| Make existing journeys completable and discoverable | Completion evidence across existing durable capabilities without changing authority or storage semantics. | [#3132](https://github.com/ugoite/ugoite/issues/3132), [#3120](https://github.com/ugoite/ugoite/issues/3120), [#3130](https://github.com/ugoite/ugoite/issues/3130) |
| Align cross-surface behavior and validation presentation | Evidence that shared business outcomes and surface-owned presentation behave as intended. | [#3132](https://github.com/ugoite/ugoite/issues/3132), [#3120](https://github.com/ugoite/ugoite/issues/3120), [#3130](https://github.com/ugoite/ugoite/issues/3130) |
| Complete recovery flows and keep docs aligned | User-visible done states and doc correctness evidence. | [#3132](https://github.com/ugoite/ugoite/issues/3132), [#3124](https://github.com/ugoite/ugoite/issues/3124) |
| Cover Product UX outcomes with focused regression tests | Exact test selectors and a recorded run for each accepted outcome. | [#3132](https://github.com/ugoite/ugoite/issues/3132); record runs separately from static projection. |
| Keep diagnostic corpus informational; track gate promotion separately | C7 policy decision and implementation remain an explicit independent acceptance follow-up. | [#2563](https://github.com/ugoite/ugoite/issues/2563), [#3123](https://github.com/ugoite/ugoite/issues/3123) |

The shipped v0.2.0 implementation row remains shipped as documented. Its release
note and the `in_progress` version tracker are not changed by this preflight.

## Authority map and design gate

[`docs/spec/migration.md`](../../spec/migration.md#cross-surface-preflight-authority-map)
lists the canonical and partial sources by domain. No Requirement was narrowed
or created solely to fill this projection. In particular, there is no dedicated
Mitase Requirement/Criterion for Konase Host confirmation in this baseline; it
is an explicit gap pending an agreed observable outcome.

Application Definition design may be considered after both [#3120](https://github.com/ugoite/ugoite/issues/3120)
and [#3124](https://github.com/ugoite/ugoite/issues/3124) meet their exit
conditions, the preflight map can trace the existing EntryQuery, SQL, Form, and
Mutation boundaries, and no unexplained authorization or semantic-drift gap
remains. PRE-01 completion alone is not that decision. The saved representation,
Capability granularity, QueryHandle, Saved SQL's formal Konase read path, and
durable background Work stay open design questions.
