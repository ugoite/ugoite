---
title: "v0.2.1 Remote SQL export authorization acceptance"
---

This acceptance record distinguishes the existing mocked CLI failure test from
the live server authorization gate in issue
[#3140](https://github.com/ugoite/ugoite/issues/3140). A mock denial does not
prove that current Space membership is rechecked on a Remote continuation.

## Acceptance status

**Live Remote revocation: passed** on the source commit recorded below. Run
`scripts/test-sql-export-remote-auth.sh` against another checkout to repeat the
gate. The fixture creates a disposable Space, accepts a real Passkey invitation
as a viewer, approves a REST device credential, and exports through the real
`sql.query` route. A forward-only local HTTP proxy holds page two until the
owner revokes the viewer; the proxy forwards the request to the actual server
and does not generate or modify API responses.

Credential expiry also passed on source commit
`e12f3c71d9b48732e1ced1a14b4bbf57c6eeaac9`. Its isolated Remote CLI fixture
holds page one's real response until the server-issued access token expires,
then confirms the CLI sends one refresh grant and receives HTTP 200 before
fetching page two. It uses the token response's `expires_in` value and does not
change any system clock. The live revocation harness is tracked by
[#3185](https://github.com/ugoite/ugoite/issues/3185) under parent
[#3140](https://github.com/ugoite/ugoite/issues/3140). Do not include user
codes, credentials, continuation values, or private fixture data in future run
records.

## Run record

| Field                       | Result                                                                                   |
| --------------------------- | ---------------------------------------------------------------------------------------- |
| Source commit               | `4293be8e6a26d837b027132a14420d53be298c1e`                                               |
| Command                     | `scripts/test-sql-export-remote-auth.sh`                                                 |
| OS / browser / runtime      | macOS 26.6.1 arm64; Chromium 148.0.7778.96; Deno 2.8.3; Cargo 1.94.0                     |
| Server and fixture          | Local server from the source commit; disposable Space with two entries                   |
| Page 1 and live revoke      | HTTP 200; owner revoked the viewer at the page-two barrier                               |
| Page 2 response / CLI exit  | Same SQL, continuation present, exactly two requests; HTTP 403 `FORBIDDEN`; nonzero exit |
| Output and temporary files  | Final output absent; temporary export file removed                                       |
| Credential expiry           | One successful refresh after real access-token expiry; page two returned and export committed two rows |
| Sanitized artifact location | This record; no raw trace or credential-bearing log retained                             |

## Credential-expiry run record

| Field                       | Result                                                                                   |
| --------------------------- | ---------------------------------------------------------------------------------------- |
| Source commit               | `e12f3c71d9b48732e1ced1a14b4bbf57c6eeaac9`                                               |
| Command                     | `scripts/test-sql-export-remote-auth.sh`                                                 |
| OS / browser / runtime      | macOS 26.6.1 arm64; Chromium 148.0.7778.96; Deno 2.8.3; Cargo 1.94.0                     |
| Server and fixture          | Local server from the source commit; isolated viewer credential and two-entry Space       |
| Page 1 and expiry           | HTTP 200; response held until the server-issued access-token lifetime elapsed             |
| Refresh and page 2          | One refresh grant received HTTP 200; exactly two pages used the same SQL and continuation |
| Output                      | CLI exited successfully and committed two NDJSON rows                                    |
| Sanitized artifact location | This record; tokens and opaque continuation values were not retained                     |

## Pass criteria

- The viewer's approved REST device credential fetches page one for the
  immutable Space and SQL query.
- The owner revokes that viewer after the test barrier observes the page-two
  request. The unchanged request is then evaluated by the real server.
- Page two returns HTTP 403 with the server's existing `FORBIDDEN` code. The CLI
  exits nonzero, does not restart the query or switch Space/publication, commits
  no output file, cleans its temporary file, and does not print the opaque
  continuation.
- Failures during server startup, Passkey enrollment, device approval, or
  fixture setup are setup failures, not authorization passes.
- For credential expiry, the real access token expires between page one and
  page two; the CLI refreshes it once, keeps the same SQL query, and writes both
  rows. No shared clock is changed.

The harness proves the tested server's live per-page membership check for this
fixture and request path. The expiry run proves the successful refresh path for
this fixture; it does not cover expired refresh credentials, every ACL shape,
or authorization behavior for other deployments.
