---
title: "v0.2.1 virtual result table assistive-technology acceptance"
---

This record separates automated rendering checks from manual screen-reader
acceptance. Automated DOM and keyboard assertions are not a substitute for a
real VoiceOver or NVDA session.

## Acceptance status

**Manual acceptance: unverified.** No VoiceOver or NVDA result is recorded here.
Issue [#3148](https://github.com/ugoite/ugoite/issues/3148) remains open until a
manual run completes every required case below and records its environment and
evidence. Do not describe the virtual result table as screen-reader verified
before then.

Automated checks are recorded separately:

- `frontend/src/components/PagedResultTable.test.tsx` checks native table
  semantics, viewport-bounded rows, hidden virtual spacer rows, page-local row
  coordinates, keyboard focus movement, and fallback rendering.
- `e2e/qry02-visible-rows.test.ts` covers a 1,000-row synthetic fixture in
  Chromium. It does not use a screen reader or prove behavior with real 10k
  Space data.

## Manual run record

Complete this section for each actual run. Use a synthetic or otherwise
non-sensitive fixture and preserve the raw checklist and any recording outside
the repository unless it is safe to publish.

| Field | Result |
| --- | --- |
| Source commit | Not run |
| Operating system and version | Not run |
| Browser and version | Not run |
| Screen reader and version | Not run |
| Device | Not run |
| Fixture | Not run |
| Evidence location | Not run |

## Required interaction matrix

Open EntryBrowser and Saved SQL with the same non-sensitive fixture. Repeat the
required cases at viewport heights of 480px and 720px. Record observed speech,
focus target, pass/fail, and evidence for each case.

| Case | EntryBrowser | Saved SQL | 480px | 720px | Result / evidence |
| --- | --- | --- | --- | --- | --- |
| Column headers and cells are announced in a usable order | ☐ | ☐ | ☐ | ☐ | Not run |
| Page-local row position and row count are understandable | ☐ | ☐ | ☐ | ☐ | Not run |
| Virtual spacer rows are absent from spoken table navigation | ☐ | ☐ | ☐ | ☐ | Not run |
| EntryBrowser ArrowUp/ArrowDown reaches an offscreen row and retains focus | ☐ | N/A | ☐ | ☐ | Not run |
| Saved SQL scroll region works with keyboard navigation and does not claim grid interaction | N/A | ☐ | ☐ | ☐ | Not run |
| Page change, loading, error, empty, and retry preserve focus and convey state | ☐ | ☐ | ☐ | ☐ | Not run |

Also record optional keyboard-only checks at 320–390px width and 200% zoom.
Keep those results separate from the required 480px / 720px screen-reader
matrix.

## Existing synthetic evidence

[`qry-02-measurement.md`](qry-02-measurement.md) records the prior 1,000-row
Chromium / Playwright run and explicitly distinguishes it from real Space data
and manual assistive-technology acceptance. It does not close the manual gate
in #3148.
