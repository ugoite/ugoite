version: "0.2"
updated: "2026-07-17"
page:
  id: space-entries-object
  title: Entries Compatibility Route
  route: /spaces/{space_id}/entries
  implementation: implemented
components:
  body:
    - id: plain-entry-list
      type: entry-card-grid
      description: The plain Entries route loads the Space's Entry list and exposes the first-entry flow without redirecting to Forms.
    - id: sql-session-results
      type: query-results
      condition: session query parameter is present
      description: Cross-Form SQL session results remain available at this route.
