category: Link Management
prefix: REQ-LNK

requirements:
  - id: REQ-LNK-004
    title: Normalize Ugoite Link URIs
    description: |
      Markdown content MUST normalize Ugoite link URIs (e.g. entry and asset
      links) into canonical ugoite:// forms during create/update.
    priority: medium
    status: implemented
    tests:
      rust:
        - file: ugoite-core/tests/test_entry.rs
          tests:
            - test_entry_req_lnk_004_normalize_ugoite_link_uris
