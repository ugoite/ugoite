category: Form Management
prefix: REQ-FORM
requirements:
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-001
  title: List Column Types
  description: 'CLI and Server MUST expose available column types for form creation.

    '
  related_spec:
  - api/rest.md#endpoints
  - data-model/overview.md#forms-and-entries
  priority: medium
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_cli_form.rs
  - file: crates/ugoite-cli/tests/test_forms.rs
  - file: crates/ugoite-iceberg/tests/test_form.rs
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-002
  title: Form Migration Strategy
  description: 'When updating a form definition, the system MUST allow specifying

    migration strategies for existing fields (for example: set to null,

    set a default value, or keep as-is).

    '
  related_spec:
  - api/rest.md#endpoints
  - data-model/overview.md#forms-and-entries
  priority: medium
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_cli_form.rs
  - file: crates/ugoite-cli/tests/test_forms.rs
  - file: crates/ugoite-iceberg/tests/test_form.rs
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-003
  title: Form CRUD via API
  description: 'Server MUST allow creating, listing, and retrieving form definitions,

    and validate entry updates against missing form definitions.

    '
  related_spec:
  - api/rest.md#endpoints
  - data-model/overview.md#forms-and-entries
  priority: medium
  status: implemented
  verification: untraced
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-004
  title: Extra Attributes Policy
  description: 'Form definitions MUST declare how extra attributes (non-registered H2

    sections) are handled, and the system MUST enforce the policy on entry

    creation and update.

    '
  related_spec:
  - data-model/overview.md#forms-and-entries
  - data-model/file-schemas.yaml#form_definition
  priority: medium
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_entry.rs
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-005
  title: Reserved Metadata Columns
  description: 'Form definitions MUST reject field names that collide with

    system-owned metadata columns (e.g., title, form, tags). The

    reserved list is system-owned and may expand over time.

    '
  related_spec:
  - data-model/overview.md#forms-and-entries
  - data-model/file-schemas.yaml#form_definition
  priority: high
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_form.rs
  - file: crates/ugoite-domain/tests/test_coverage.rs
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-006
  title: Reserved Metadata Forms
  description: 'System-owned metadata Forms (e.g., SQL) MUST be reserved and rejected

    when users attempt to create or update Forms with those names.

    '
  related_spec:
  - data-model/overview.md#forms-and-entries
  - features/sql.md#ugoite-sql
  priority: high
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_form.rs
  - file: crates/ugoite-domain/tests/test_coverage.rs
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-007
  title: Row reference field targets
  description: 'Form fields with type row_reference MUST declare a target_form that

    resolves to an existing Form or the current Form.

    '
  related_spec:
  - data-model/overview.md#markdown-mapping
  - data-model/file-schemas.yaml#form_definition
  - api/rest.md#endpoints
  priority: high
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_form.rs
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-008
  title: Reserved Identity Metadata Forms
  description: 'System MUST reserve metadata Form names `User` and `UserGroup` in

    addition to existing metadata Forms, and MUST reject user-authored Form

    definitions that collide with reserved metadata Form names.

    '
  related_spec:
  - data-model/overview.md#forms-and-entries
  priority: high
  status: planned
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_form_reserved_identity.rs
- set_id: REQCAT-FORM
  source_file: requirements/form.yaml
  scope: Form structure, validation, and lifecycle requirements.
  linked_policies:
  - POL-007
  linked_specifications:
  - SPEC-DM-SCHEMA
  - SPEC-STORIES-CORE
  - SPEC-UI-PAGES
  id: REQ-FORM-009
  title: Mandatory Attribution Metadata
  description: 'All Form-backed rows MUST persist system-managed attribution metadata.

    At minimum, `author` and `updated_by` MUST be set from authenticated user

    identity, with `updated_by` representing the latest writer.

    '
  related_spec:
  - data-model/overview.md#forms-and-entries
  - data-model/directory-structure.md#iceberg-managed-form-storage
  priority: high
  status: planned
  verification: untraced
