# Asset Management Requirements

category: Asset Management
prefix: REQ-ASSET

requirements:
  - id: REQ-ASSET-001
    title: Asset Creation
    description: |
      Store binary files (images, PDF, etc.) in the assets/ folder.
    related_spec:
      - api/rest.md#upload-asset
      - data-model/directory-structure.md
    priority: medium
    status: implemented
    tests:
      pytest:
        - file: ugoite-cli/tests/test_assets.py
          tests:
            - test_asset_lifecycle
      rust:
        - file: ugoite-core/tests/test_asset.rs
          tests:
            - test_asset_req_asset_001_create_asset
            - test_asset_req_asset_001_delete_asset
