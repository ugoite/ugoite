category: Indexer
prefix: REQ-IDX
requirements:
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-001
  title: Persistent derived-index rebuild
  description: A future reindex operation may materialize derived indexes from Iceberg Entry tables. In the current
    release `reindex_all` returns an explicit not-implemented error; only direct query/stat aggregation is available.
  related_spec:
  - architecture/overview.md#architecture-overview
  - data-model/overview.md#search-query-and-derived-data
  priority: high
  status: planned
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_indexer.rs
  - file: crates/ugoite-iceberg/tests/test_index.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-002
  title: Form Validation
  description: 'Validate properties against form definitions and generate warnings.

    '
  related_spec:
  - stories/core.yaml#STORY-002
  - data-model/overview.md#forms-and-entries
  priority: medium
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_indexer.rs
  - file: crates/ugoite-iceberg/tests/test_index.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-003
  title: Structured Queries
  description: 'Execute structured queries against the index.

    '
  related_spec:
  - stories/core.yaml#STORY-005
  - api/rest.md#endpoints
  priority: high
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_indexer.rs
  - file: crates/ugoite-iceberg/tests/test_index.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-004
  title: Persistent inverted-index generation
  description: Generating and storing a persistent inverted index remains planned. Current tests assert that `ugoite
    index run` fails explicitly and does not create index files.
  related_spec:
  - data-model/overview.md#search-query-and-derived-data
  priority: medium
  status: planned
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_indexer.rs
  - file: crates/ugoite-iceberg/tests/test_index.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-005
  title: Word-count calculation
  description: Word count is computed by the shared text/domain helper and exposed in derived query records. It
    is not evidence that a persistent index rebuild exists.
  related_spec:
  - data-model/overview.md#markdown-mapping
  priority: low
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_indexer.rs
  - file: crates/ugoite-iceberg/tests/test_index.rs
  - file: crates/ugoite-domain/tests/test_coverage.rs
  - file: crates/ugoite-domain/tests/test_text.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-006
  title: Indexing watch loop
  description: A watch loop that performs per-entry persistent index updates is not implemented. The CLI exposes
    the command surface, but the core update operation returns a not-implemented error.
  related_spec:
  - architecture/overview.md#architecture-overview
  priority: low
  status: planned
  verification: traced
  tests:
  - file: crates/ugoite-cli/tests/test_indexer.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-007
  title: Keyword scan search endpoint
  description: GET /spaces/{space_id}/search performs case-insensitive substring matching by scanning current non-deleted
    Entry rows. It does not consult a persistent inverted index and does not rank by relevance.
  related_spec:
  - stories/core.yaml#STORY-005
  - api/rest.md#endpoints
  priority: medium
  status: implemented
  verification: untraced
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-008
  title: Ugoite SQL Query Execution
  description: 'Execute read-only DataFusion SQL queries against authorized Iceberg Form relations via the

    existing /spaces/{space_id}/query endpoint.

    '
  related_spec:
  - features/sql.md
  - api/rest.md#endpoints
  priority: high
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_index.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-009
  title: Ugoite SQL JOIN Support
  description: 'Ugoite SQL MUST support DataFusion joins between authorized Form relations.

    JSON-backed entries, links, and assets SQL tables are not part of the SQL surface.

    '
  related_spec:
  - features/sql.md
  priority: high
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_index.rs
- set_id: REQCAT-INDEX
  source_file: requirements/index.yaml
  scope: Structured extraction plus derived index/query artifact behavior requirements.
  linked_policies:
  - POL-001
  - POL-002
  - POL-012
  linked_specifications:
  - SPEC-DM-OVERVIEW
  - SPEC-FEATURES-SQL
  - SPEC-API-REST
  id: REQ-IDX-010
  title: Rich Content Column Parsing
  description: 'Validation MUST parse Markdown into expanded content column types,

    including lists, booleans, integers, floats, timestamps, time, UUIDs,

    and binary payloads.

    '
  related_spec:
  - data-model/overview.md#markdown-mapping
  priority: high
  status: implemented
  verification: traced
  tests:
  - file: crates/ugoite-iceberg/tests/test_index.rs
