schema: mitase/spec/v1
kind: features
namespace: security
category: Server security boundary surfaces
features:
  - id: FEAT-SEC-001
    title: Server security boundary
    summary: Connect loopback-first server configuration, response security headers, mandatory REST/MCP authentication, deployment artifacts, and exact verification evidence without introducing a Ugoite-specific specification kind.
    status: implemented
    bindings:
      - id: server
        role: implementation
        facet: backend
        responsibility: Enforce the response security-header policy and authenticated boundary for protected REST and MCP requests.
        targets:
          - id: security-header-policy
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: SecurityHeadersPolicy }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-002#criterion.common-header-policy
              - kind: satisfies
                criterion: REQ-SEC-002#criterion.loopback-hsts-boundary
          - id: security-route-layer
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: app_layers }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-002#criterion.common-header-policy
          - id: authentication-middleware
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: require_auth }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-003#criterion.protected-rest
          - id: mcp-authentication
            adapter: rust
            path: crates/ugoite-server/src/mcp.rs
            selector: { kind: symbol, name: authenticate }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-003#criterion.protected-mcp
      - id: listen-configuration
        role: implementation
        facet: deployment
        responsibility: Keep the server's explicit listen-address override and loopback default visible at the process and operator configuration boundaries.
        targets:
          - id: server-entrypoint
            adapter: rust
            path: crates/ugoite-server/src/main.rs
            selector: { kind: symbol, name: server_address_from }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-001#criterion.default-loopback
          - id: release-compose
            adapter: declared
            path: docker-compose.release.yaml
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-001#criterion.compose-loopback
      - id: contract
        role: contract-source
        facet: api
        responsibility: Keep the authoritative OpenAPI artifact connected to the protected REST operation surface; detailed authentication operation coverage remains part of the next Security migration slices.
        owns:
          - id: openapi-source
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: file }
        targets:
          - id: protected-space-list
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: /spaces }

  - id: FEAT-SEC-002
    title: Human Passkey and initial bootstrap
    summary: Connect the passwordless Passkey/WebAuthn authentication flow and initial node-owner bootstrap across identity state, server handlers, browser routes, OpenAPI operations, and exact verification artifacts.
    status: implemented
    bindings:
      - id: identity
        role: implementation
        facet: identity
        responsibility: Enforce discoverable Passkey registration, server-side assertion validation, one-time setup-secret handling, initial administrator creation, recovery-code emission, and opaque session creation.
        targets:
          - id: passkey-registration
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: start_add_passkey }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.discoverable-credentials
          - id: passkey-authentication
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: finish_authentication }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.server-side-assertion-validation
          - id: setup-secret
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: bootstrap_if_needed }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.setup-secret-hash-once
          - id: setup-finish
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: finish_setup_registration }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.initial-owner-passkey
          - id: session-validation
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: authenticate_session }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.opaque-bootstrap-session
      - id: server
        role: implementation
        facet: backend
        responsibility: Expose the Passkey login and setup ceremonies through the server's public authentication boundary.
        targets:
          - id: passkey-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: auth_passkey_start }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.passwordless-primary
          - id: passkey-registration-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: start_add_passkey }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.discoverable-credentials
          - id: passkey-registration-finish
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: finish_add_passkey }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.discoverable-credentials
          - id: passkey-finish
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: auth_passkey_finish }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.server-side-assertion-validation
          - id: setup-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: auth_setup_start }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.initial-owner-passkey
          - id: setup-finish
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: auth_setup_finish }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.initial-owner-passkey
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.bootstrap-recovery-material
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.opaque-bootstrap-session
      - id: client
        role: implementation
        facet: frontend
        responsibility: Keep the browser Passkey login and initial setup journeys connected to the server authentication API while preserving the requested post-authentication route.
        targets:
          - id: auth-client
            adapter: typescript
            path: frontend/src/lib/auth-api.ts
            selector: { kind: symbol, name: authApi }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.passwordless-primary
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.bootstrap-recovery-material
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: login-route
            adapter: typescript
            path: frontend/src/routes/login.tsx
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-004#criterion.passwordless-primary
          - id: setup-route
            adapter: typescript
            path: frontend/src/routes/setup.tsx
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-005#criterion.bootstrap-recovery-material
      - id: contract
        role: contract-source
        facet: api
        responsibility: Keep the public OpenAPI authentication operations connected to the Passkey and setup handlers.
        targets:
          - id: setup-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/setup/start }
          - id: setup-finish
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/setup/finish }
          - id: passkey-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/passkey/start }
          - id: passkey-finish
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/passkey/finish }
          - id: passkey-registration-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/passkeys/start }
          - id: passkey-registration-finish
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/passkeys/finish }
    contracts:
      - id: passkey-authentication
        kind: http
        source: FEAT-SEC-002#binding.contract/target.passkey-start
        participants:
          - target: FEAT-SEC-002#binding.identity/target.passkey-authentication
            role: identity
          - target: FEAT-SEC-002#binding.server/target.passkey-start
            role: backend
          - target: FEAT-SEC-002#binding.server/target.passkey-finish
            role: backend
          - target: FEAT-SEC-002#binding.client/target.auth-client
            role: client
          - target: FEAT-SEC-002#binding.client/target.login-route
            role: route
        guarantees:
          - REQ-SEC-004#criterion.passwordless-primary
          - REQ-SEC-004#criterion.server-side-assertion-validation
      - id: passkey-registration
        kind: http
        source: FEAT-SEC-002#binding.contract/target.passkey-registration-start
        participants:
          - target: FEAT-SEC-002#binding.identity/target.passkey-registration
            role: identity
          - target: FEAT-SEC-002#binding.server/target.passkey-registration-start
            role: backend
          - target: FEAT-SEC-002#binding.server/target.passkey-registration-finish
            role: backend
        guarantees: [REQ-SEC-004#criterion.discoverable-credentials]
      - id: initial-bootstrap
        kind: http
        source: FEAT-SEC-002#binding.contract/target.setup-start
        participants:
          - target: FEAT-SEC-002#binding.identity/target.setup-secret
            role: identity
          - target: FEAT-SEC-002#binding.identity/target.setup-finish
            role: identity
          - target: FEAT-SEC-002#binding.server/target.setup-start
            role: backend
          - target: FEAT-SEC-002#binding.server/target.setup-finish
            role: backend
          - target: FEAT-SEC-002#binding.client/target.auth-client
            role: client
          - target: FEAT-SEC-002#binding.client/target.setup-route
            role: route
          - target: FEAT-SEC-002#binding.identity/target.session-validation
            role: session
        guarantees:
          - REQ-SEC-005#criterion.setup-secret-hash-once
          - REQ-SEC-005#criterion.initial-owner-passkey
          - REQ-SEC-005#criterion.bootstrap-recovery-material
          - REQ-SEC-005#criterion.opaque-bootstrap-session

  - id: FEAT-SEC-003
    title: Account Self-Recovery
    summary: Connect recovery-only TOTP enrollment and the Recovery Code plus TOTP Passkey replacement flow across Node identity state, server boundaries, browser recovery UI, OpenAPI, and the end-to-end recovery journey.
    status: implemented
    bindings:
      - id: identity
        role: implementation
        facet: identity
        responsibility: Enforce encrypted recovery-factor storage, dual-factor recovery validation, credential-generation replacement, Recovery Code rotation, stale-authority invalidation, and recovery session creation.
        targets:
          - id: totp-enrollment
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: start_totp_enrollment }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.recovery-factor-enrollment
          - id: totp-enrollment-finish
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: finish_totp_enrollment }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.recovery-factor-enrollment
          - id: recovery-start
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: start_recovery_registration }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.dual-factor-recovery
          - id: recovery-finish
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: finish_recovery_registration }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.credential-replacement
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.stale-authority-invalidation
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.recovery-removes-oidc
          - id: recovery-code-rotation
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: rotate_recovery_codes_with_snapshot_credential_and_session }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.credential-replacement
      - id: server
        role: implementation
        facet: backend
        responsibility: Expose recovery-factor enrollment, Account Self-Recovery, and Node audit reads through the authenticated server boundary.
        targets:
          - id: totp-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: start_totp_enrollment }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.recovery-factor-enrollment
          - id: totp-finish
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: finish_totp_enrollment }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.recovery-factor-enrollment
          - id: recovery-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: auth_recovery_start }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.dual-factor-recovery
          - id: recovery-finish
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: auth_recovery_finish }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.credential-replacement
          - id: node-audit
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: list_node_audit_events }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.recovery-audit-attribution
      - id: client
        role: implementation
        facet: frontend
        responsibility: Keep the browser Account Self-Recovery journey connected to the recovery API and display rotated Recovery Codes once before returning to the requested route.
        targets:
          - id: account-recovery-route
            adapter: typescript
            path: frontend/src/routes/recover/account.tsx
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.credential-replacement
          - id: browser-recovery-journey
            adapter: typescript
            path: e2e/account-self-recovery.test.ts
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-014#criterion.recovery-audit-attribution
      - id: contract
        role: contract-source
        facet: api
        responsibility: Keep the public OpenAPI recovery operations and Node audit read connected to the recovery implementation boundaries.
        targets:
          - id: totp-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/recovery/totp/start }
          - id: totp-finish
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/recovery/totp/finish }
          - id: recovery-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/recovery/start }
          - id: recovery-finish
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/recovery/finish }
          - id: node-audit
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: /auth/audit }
    contracts:
      - id: account-self-recovery
        kind: http
        source: FEAT-SEC-003#binding.contract/target.recovery-start
        participants:
          - target: FEAT-SEC-003#binding.identity/target.recovery-start
            role: identity
          - target: FEAT-SEC-003#binding.identity/target.recovery-finish
            role: identity
          - target: FEAT-SEC-003#binding.identity/target.recovery-code-rotation
            role: identity
          - target: FEAT-SEC-003#binding.server/target.recovery-start
            role: backend
          - target: FEAT-SEC-003#binding.server/target.recovery-finish
            role: backend
          - target: FEAT-SEC-003#binding.client/target.account-recovery-route
            role: client
          - target: FEAT-SEC-002#binding.client/target.auth-client
            role: api-client
        guarantees:
          - REQ-SEC-014#criterion.dual-factor-recovery
          - REQ-SEC-014#criterion.credential-replacement
          - REQ-SEC-014#criterion.stale-authority-invalidation
      - id: recovery-factor-enrollment
        kind: http
        source: FEAT-SEC-003#binding.contract/target.totp-start
        participants:
          - target: FEAT-SEC-003#binding.identity/target.totp-enrollment
            role: identity
          - target: FEAT-SEC-003#binding.identity/target.totp-enrollment-finish
            role: identity
          - target: FEAT-SEC-003#binding.server/target.totp-start
            role: backend
          - target: FEAT-SEC-003#binding.server/target.totp-finish
            role: backend
        guarantees:
          - REQ-SEC-014#criterion.recovery-factor-enrollment
      - id: recovery-audit
        kind: http
        source: FEAT-SEC-003#binding.contract/target.node-audit
        participants:
          - target: FEAT-SEC-003#binding.server/target.node-audit
            role: backend
        guarantees:
          - REQ-SEC-014#criterion.recovery-audit-attribution

  - id: FEAT-SEC-004
    title: Space authorization, membership, and audit
    summary: Connect Space-scoped authorization, invitation membership lifecycle, and owner-authorized audit reads across the shared authorization state, identity invitation flow, Rust service, server handlers, OpenAPI operations, browser routes, and exact repository artifacts.
    status: implemented
    bindings:
      - id: authorization
        role: implementation
        facet: authorization
        responsibility: Evaluate Space roles, explicit grants, resource parents, and lifecycle state before protected resource operations are admitted.
        targets:
          - id: authorizer
            adapter: rust
            path: crates/ugoite-iceberg/src/authorization.rs
            selector: { kind: symbol, name: Authorizer }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-006#criterion.scoped-resource-actions
              - kind: satisfies
                criterion: REQ-SEC-006#criterion.grant-only-inheritance
          - id: authorization-decision
            adapter: rust
            path: crates/ugoite-iceberg/src/authorization.rs
            selector: { kind: symbol, name: effective_actions_for_state }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-006#criterion.scoped-resource-actions
              - kind: satisfies
                criterion: REQ-SEC-006#criterion.grant-only-inheritance
          - id: authorized-resource-filter
            adapter: rust
            path: crates/ugoite-iceberg/src/authorization.rs
            selector: { kind: symbol, name: filter_authorized_resources }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-006#criterion.filtered-before-materialization
          - id: service-authorization-boundary
            adapter: rust
            path: crates/ugoite-iceberg/src/service.rs
            selector: { kind: symbol, name: filter_json_resources_authorized_for_principals }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-006#criterion.filtered-before-materialization
      - id: membership
        role: implementation
        facet: identity
        responsibility: Issue and accept Space invitations while preserving one principal per existing account or Space binding.
        targets:
          - id: invitation-issue
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: issue_invitation }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.invitation-acceptance
          - id: invitation-acceptance
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: accept_invitation_for_account }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.invitation-acceptance
          - id: invitation-finalization
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: finalize_invitation_binding }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.invitation-acceptance
      - id: membership-server
        role: implementation
        facet: backend
        responsibility: Expose authenticated membership listing, invitation issuance, role changes, and revocation through the protected Space server boundary.
        targets:
          - id: space-permission
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: require_space_action }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-006#criterion.scoped-resource-actions
          - id: members-list
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: list_members }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.explicit-membership-state
          - id: member-invite
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: invite_member }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.invitation-acceptance
          - id: member-role-update
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: update_member_role }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.concurrency-safe-membership
          - id: member-revoke
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: revoke_member }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.explicit-membership-state
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.concurrency-safe-membership
      - id: audit
        role: implementation
        facet: audit
        responsibility: Append integrity-checked, attributed Space audit events and expose filtered audit reads only after owner-level authorization.
        targets:
          - id: audit-append
            adapter: rust
            path: crates/ugoite-iceberg/src/audit.rs
            selector: { kind: symbol, name: append_audit_event }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-008#criterion.attributed-append-only-audit
          - id: audit-list
            adapter: rust
            path: crates/ugoite-iceberg/src/audit.rs
            selector: { kind: symbol, name: list_audit_events }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-008#criterion.owner-authorized-audit-read
          - id: audit-server
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: list_audit_events }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-008#criterion.owner-authorized-audit-read
      - id: contract
        role: contract-source
        facet: api
        responsibility: Keep the authenticated membership and Space audit operations connected to the authoritative OpenAPI artifact.
        targets:
          - id: members-list
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: '/spaces/{space_id}/members' }
          - id: member-invite
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: '/spaces/{space_id}/members/invitations' }
          - id: member-role-update
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: '/spaces/{space_id}/members/{principal_id}/role' }
          - id: member-revoke
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: delete, path: '/spaces/{space_id}/members/{principal_id}' }
          - id: space-audit
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: '/spaces/{space_id}/audit' }
      - id: client
        role: implementation
        facet: frontend
        responsibility: Keep invitation acceptance connected to the protected browser route.
        targets:
          - id: invitation-route
            adapter: typescript
            path: frontend/src/routes/spaces/join.tsx
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-007#criterion.invitation-acceptance
    contracts:
      - id: space-authorization
        kind: http
        source: FEAT-SEC-004#binding.contract/target.members-list
        participants:
          - target: FEAT-SEC-004#binding.authorization/target.authorizer
            role: authorization
          - target: FEAT-SEC-004#binding.authorization/target.authorization-decision
            role: authorization
          - target: FEAT-SEC-004#binding.authorization/target.authorized-resource-filter
            role: authorization
          - target: FEAT-SEC-004#binding.authorization/target.service-authorization-boundary
            role: service
          - target: FEAT-SEC-004#binding.membership-server/target.space-permission
            role: backend
        guarantees:
          - REQ-SEC-006#criterion.scoped-resource-actions
          - REQ-SEC-006#criterion.grant-only-inheritance
          - REQ-SEC-006#criterion.filtered-before-materialization
      - id: membership-lifecycle
        kind: http
        source: FEAT-SEC-004#binding.contract/target.member-invite
        participants:
          - target: FEAT-SEC-004#binding.membership/target.invitation-issue
            role: identity
          - target: FEAT-SEC-004#binding.membership/target.invitation-acceptance
            role: identity
          - target: FEAT-SEC-004#binding.membership/target.invitation-finalization
            role: identity
          - target: FEAT-SEC-004#binding.membership-server/target.members-list
            role: backend
          - target: FEAT-SEC-004#binding.membership-server/target.member-invite
            role: backend
          - target: FEAT-SEC-004#binding.membership-server/target.member-role-update
            role: backend
          - target: FEAT-SEC-004#binding.membership-server/target.member-revoke
            role: backend
          - target: FEAT-SEC-004#binding.client/target.invitation-route
            role: client
        guarantees:
          - REQ-SEC-007#criterion.explicit-membership-state
          - REQ-SEC-007#criterion.invitation-acceptance
          - REQ-SEC-007#criterion.concurrency-safe-membership
      - id: audit-visibility
        kind: http
        source: FEAT-SEC-004#binding.contract/target.space-audit
        participants:
          - target: FEAT-SEC-004#binding.audit/target.audit-append
            role: audit
          - target: FEAT-SEC-004#binding.audit/target.audit-list
            role: audit
          - target: FEAT-SEC-004#binding.audit/target.audit-server
            role: backend
          - target: FEAT-SEC-004#binding.membership-server/target.space-permission
            role: authorization
        guarantees:
          - REQ-SEC-008#criterion.attributed-append-only-audit
          - REQ-SEC-008#criterion.owner-authorized-audit-read

  - id: FEAT-SEC-005
    title: OIDC external identity authentication and linking
    summary: Connect the OIDC authorization-code flow, invitation-gated account creation, federated session boundary, protected external identity management, recovery interaction, and exact identity, server, frontend, and OpenAPI evidence without introducing a Ugoite-specific specification kind.
    status: implemented
    bindings:
      - id: identity
        role: implementation
        facet: identity
        responsibility: Enforce provider discovery and validation, one-shot OIDC attempts, exact issuer-subject identity linking, invitation-gated account creation, federated sessions, first-Passkey bootstrap, protected unlinking, and recovery interaction.
        targets:
          - id: provider-configuration
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: configure_oidc_provider }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.secure-oidc-protocol
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: attempt-creation
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: save_oidc_attempt_with_session }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.secure-oidc-protocol
          - id: attempt-consumption
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: consume_oidc_attempt }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.secure-oidc-protocol
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: login-completion
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: complete_oidc_login }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.exact-external-identity
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.invitation-gated-account
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: first-passkey-bootstrap
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: start_bootstrap_passkey }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
          - id: first-passkey-finish
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: finish_bootstrap_passkey }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
          - id: link-list
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: list_oidc_links }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: unlink
            adapter: rust
            path: crates/ugoite-identity/src/node_identity.rs
            selector: { kind: symbol, name: unlink_oidc }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
      - id: server
        role: implementation
        facet: backend
        responsibility: Expose OIDC provider administration, authorization starts, callbacks, first-Passkey bootstrap, and protected link management through the authenticated server boundary.
        targets:
          - id: provider-config
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: configure_oidc_provider }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.secure-oidc-protocol
          - id: provider-list
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: list_oidc_providers }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: provider-disable
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: disable_oidc_provider }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: authorization-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: start_oidc_authorization }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.secure-oidc-protocol
          - id: link-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: oidc_link_start }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: oidc-callback
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: oidc_callback }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.secure-oidc-protocol
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
          - id: first-passkey-start
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: start_bootstrap_passkey }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
          - id: first-passkey-finish
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: finish_bootstrap_passkey }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
          - id: link-list
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: list_oidc_links }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
          - id: link-remove
            adapter: rust
            path: crates/ugoite-server/src/lib.rs
            selector: { kind: symbol, name: unlink_oidc }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
      - id: client
        role: implementation
        facet: frontend
        responsibility: Keep OIDC login continuation, provider choices, and protected external identity actions behind the current browser authentication and security settings journeys.
        targets:
          - id: safe-continuation
            adapter: typescript
            path: frontend/src/lib/auth-route.ts
            selector: { kind: symbol, name: getSafeNextPath }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
          - id: login-route
            adapter: typescript
            path: frontend/src/routes/login.tsx
            selector: { kind: symbol, name: LoginRoute }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.federated-session-boundary
          - id: security-settings
            adapter: typescript
            path: frontend/src/routes/settings/security.tsx
            selector: { kind: symbol, name: SecuritySettingsRoute }
            claims:
              - kind: satisfies
                criterion: REQ-SEC-016#criterion.protected-link-management
      - id: contract
        role: contract-source
        facet: api
        responsibility: Keep the OIDC provider, authorization, callback, link, and invitation first-Passkey operations connected to the authoritative OpenAPI artifact.
        targets:
          - id: provider-list
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: /auth/oidc/providers }
          - id: provider-config
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/oidc/providers }
          - id: provider-disable
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: delete, path: '/auth/oidc/providers/{provider_id}' }
          - id: link-list
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: /auth/oidc/links }
          - id: link-remove
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: delete, path: '/auth/oidc/links/{method_id}' }
          - id: authorization-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: '/auth/oidc/{provider_id}/start' }
          - id: authorization-link-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: '/auth/oidc/{provider_id}/link' }
          - id: callback
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: get, path: /auth/oidc/callback }
          - id: first-passkey-start
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/passkeys/bootstrap/start }
          - id: first-passkey-finish
            adapter: openapi
            path: crates/ugoite-server/src/openapi.json
            selector: { kind: operation, method: post, path: /auth/passkeys/bootstrap/finish }
    contracts:
      - id: oidc-authentication
        kind: http
        source: FEAT-SEC-005#binding.contract/target.authorization-start
        participants:
          - target: FEAT-SEC-005#binding.identity/target.provider-configuration
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.attempt-creation
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.attempt-consumption
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.login-completion
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.first-passkey-bootstrap
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.first-passkey-finish
            role: identity
          - target: FEAT-SEC-005#binding.server/target.provider-config
            role: backend
          - target: FEAT-SEC-005#binding.server/target.authorization-start
            role: backend
          - target: FEAT-SEC-005#binding.server/target.oidc-callback
            role: backend
          - target: FEAT-SEC-005#binding.server/target.first-passkey-start
            role: backend
          - target: FEAT-SEC-005#binding.server/target.first-passkey-finish
            role: backend
          - target: FEAT-SEC-002#binding.client/target.auth-client
            role: client
          - target: FEAT-SEC-005#binding.client/target.safe-continuation
            role: continuation
          - target: FEAT-SEC-005#binding.client/target.login-route
            role: route
          - target: FEAT-SEC-005#binding.contract/target.authorization-start
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.callback
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.first-passkey-start
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.first-passkey-finish
            role: contract
        guarantees:
          - REQ-SEC-016#criterion.secure-oidc-protocol
          - REQ-SEC-016#criterion.exact-external-identity
          - REQ-SEC-016#criterion.invitation-gated-account
          - REQ-SEC-016#criterion.federated-session-boundary
      - id: oidc-link-management
        kind: http
        source: FEAT-SEC-005#binding.contract/target.link-list
        participants:
          - target: FEAT-SEC-003#binding.identity/target.recovery-finish
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.login-completion
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.link-list
            role: identity
          - target: FEAT-SEC-005#binding.identity/target.unlink
            role: identity
          - target: FEAT-SEC-005#binding.server/target.provider-list
            role: backend
          - target: FEAT-SEC-005#binding.server/target.provider-disable
            role: backend
          - target: FEAT-SEC-005#binding.server/target.link-list
            role: backend
          - target: FEAT-SEC-005#binding.server/target.link-remove
            role: backend
          - target: FEAT-SEC-005#binding.server/target.link-start
            role: backend
          - target: FEAT-SEC-005#binding.client/target.security-settings
            role: client
          - target: FEAT-SEC-002#binding.client/target.auth-client
            role: api-client
          - target: FEAT-SEC-005#binding.contract/target.provider-list
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.provider-config
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.provider-disable
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.link-list
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.link-remove
            role: contract
          - target: FEAT-SEC-005#binding.contract/target.authorization-link-start
            role: contract
        guarantees:
          - REQ-SEC-016#criterion.protected-link-management
          - REQ-SEC-016#criterion.recovery-removes-oidc
