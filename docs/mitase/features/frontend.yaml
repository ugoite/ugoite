schema: mitase/spec/v1
kind: features
namespace: frontend
category: Frontend Space selection and preference surfaces
features:
  - id: FEAT-FE-001
    title: Space selection and portable preference persistence
    summary: Connect the browser Space selector, deterministic authorized Space store, portable preference hydration, local fallback, and the /spaces route to exact frontend verification evidence.
    status: implemented
    bindings:
      - id: frontend
        role: implementation
        facet: frontend
        responsibility: Keep Space selection and portable selected-Space preference behavior connected to the shipped frontend components, stores, local fallback, and route.
        targets:
          - id: space-selector
            adapter: typescript
            path: frontend/src/components/SpaceSelector.tsx
            selector: { kind: symbol, name: SpaceSelector }
            claims:
              - kind: satisfies
                criterion: REQ-FE-001#criterion.authorized-space-selection
              - kind: satisfies
                criterion: REQ-FE-001#criterion.switch-space
          - id: space-store
            adapter: typescript
            path: frontend/src/lib/space-store.ts
            selector: { kind: symbol, name: createSpaceStore }
            claims:
              - kind: satisfies
                criterion: REQ-FE-001#criterion.authorized-space-selection
              - kind: satisfies
                criterion: REQ-FE-002#criterion.no-implicit-creation
              - kind: satisfies
                criterion: REQ-FE-003#criterion.portable-selection
          - id: portable-preferences-store
            adapter: typescript
            path: frontend/src/lib/preferences-store.ts
            selector: { kind: symbol, name: initializePortablePreferences }
            claims:
              - kind: satisfies
                criterion: REQ-FE-003#criterion.portable-selection
              - kind: satisfies
                criterion: REQ-FE-003#criterion.local-fallback
              - kind: satisfies
                criterion: REQ-FE-044#criterion.portable-locale
          - id: local-preferences
            adapter: typescript
            path: frontend/src/lib/preferences-local.ts
            selector: { kind: symbol, name: readLocalPreferences }
            claims:
              - kind: satisfies
                criterion: REQ-FE-003#criterion.local-fallback
          - id: spaces-route
            adapter: typescript
            path: frontend/src/routes/spaces/index.tsx
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-FE-001#criterion.authorized-space-selection
              - kind: satisfies
                criterion: REQ-FE-002#criterion.no-implicit-creation
              - kind: satisfies
                criterion: REQ-FE-002#criterion.explicit-create-action
  - id: FEAT-FE-002
    title: Entry browsing and selection
    summary: Connect Entry list rendering, selected-entry highlighting, selection callbacks, and the plain Entries route to exact frontend verification evidence.
    status: implemented
    bindings:
      - id: frontend
        role: implementation
        facet: frontend
        responsibility: Keep Entry browsing and selection behavior connected to the shipped frontend list components and route.
        targets:
          - id: entry-list
            adapter: typescript
            path: frontend/src/components/EntryList.tsx
            selector: { kind: symbol, name: EntryList }
            claims:
              - kind: satisfies
                criterion: REQ-FE-004#criterion.display
              - kind: satisfies
                criterion: REQ-FE-008#criterion.selection
          - id: list-panel
            adapter: typescript
            path: frontend/src/components/ListPanel.tsx
            selector: { kind: symbol, name: ListPanel }
            claims:
              - kind: satisfies
                criterion: REQ-FE-004#criterion.display
              - kind: satisfies
                criterion: REQ-FE-008#criterion.selection
              - kind: satisfies
                criterion: REQ-FE-044#criterion.dictionary-localization
          - id: entry-store
            adapter: typescript
            path: frontend/src/lib/entry-store.ts
            selector: { kind: symbol, name: createEntryStore }
            claims:
              - kind: satisfies
                criterion: REQ-FE-008#criterion.selection
              - kind: satisfies
                criterion: REQ-FE-012#criterion.consecutive-saves
          - id: entries-route
            adapter: typescript
            path: "frontend/src/routes/spaces/[space_id]/entries/index.tsx"
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-FE-004#criterion.display
              - kind: satisfies
                criterion: REQ-FE-008#criterion.selection
  - id: FEAT-FE-003
    title: Entry detail and Markdown editing
    summary: Connect the Entry detail workspace, Markdown editor, form-aware guidance, conflict handling, and safe preview behavior to exact frontend verification evidence.
    status: implemented
    bindings:
      - id: frontend
        role: implementation
        facet: frontend
        responsibility: Keep Entry detail loading, form-aware editing, Markdown preview, save state, conflict handling, and localized guidance connected to the shipped frontend components and routes.
        targets:
          - id: entry-detail-pane
            adapter: typescript
            path: frontend/src/components/EntryDetailPane.tsx
            selector: { kind: symbol, name: EntryDetailPane }
            claims:
              - kind: satisfies
                criterion: REQ-FE-009#criterion.conflict-guidance
              - kind: satisfies
                criterion: REQ-FE-010#criterion.preserve-draft
              - kind: satisfies
                criterion: REQ-FE-011#criterion.selection-and-refresh-sync
              - kind: satisfies
                criterion: REQ-FE-013#criterion.server-persistence
              - kind: satisfies
                criterion: REQ-FE-038#criterion.validation-feedback
              - kind: satisfies
                criterion: REQ-FE-052#criterion.form-derived-guidance
              - kind: satisfies
                criterion: REQ-FE-053#criterion.localized-guidance
          - id: markdown-editor
            adapter: typescript
            path: frontend/src/components/MarkdownEditor.tsx
            selector: { kind: symbol, name: MarkdownEditor }
            claims:
              - kind: satisfies
                criterion: REQ-FE-005#criterion.edit-preview-save
              - kind: satisfies
                criterion: REQ-FE-005#criterion.safe-preview
              - kind: satisfies
                criterion: REQ-FE-005A#criterion.missing-content
              - kind: satisfies
                criterion: REQ-FE-005A#criterion.new-entry-preview
              - kind: satisfies
                criterion: REQ-FE-009#criterion.conflict-guidance
          - id: markdown-utils
            adapter: typescript
            path: frontend/src/lib/markdown.ts
            selector: { kind: symbol, name: renderMarkdownPreview }
            claims:
              - kind: satisfies
                criterion: REQ-FE-005#criterion.safe-preview
          - id: entry-detail-route
            adapter: typescript
            path: "frontend/src/routes/spaces/[space_id]/entries/[entry_id]/index.tsx"
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-FE-033#criterion.detail-route
              - kind: satisfies
                criterion: REQ-FE-033#criterion.encoded-entry-id
              - kind: satisfies
                criterion: REQ-FE-034#criterion.navigation-loading
          - id: new-entry-route
            adapter: typescript
            path: "frontend/src/routes/spaces/[space_id]/entries/new.tsx"
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-FE-052#criterion.form-derived-guidance
              - kind: satisfies
                criterion: REQ-FE-030#criterion.form-aware-add-row
  - id: FEAT-FE-004
    title: Shared frontend feedback and locale surfaces
    summary: Connect structured API error presentation, shared UI localization, and portable locale preference hydration to exact frontend implementation and verification evidence.
    status: implemented
    bindings:
      - id: frontend
        role: implementation
        facet: frontend
        responsibility: Keep shared frontend error feedback and locale behavior connected to the shipped dialogs, list surfaces, API client, dictionary, and portable preference store.
        targets:
          - id: create-dialogs
            adapter: typescript
            path: frontend/src/components/create-dialogs.tsx
            selector: { kind: symbol, name: CreateFormDialog }
            claims:
              - kind: satisfies
                criterion: REQ-FE-043#criterion.readable-errors
              - kind: satisfies
                criterion: REQ-FE-044#criterion.dictionary-localization
          - id: entry-dialog
            adapter: typescript
            path: frontend/src/components/create-dialogs.tsx
            selector: { kind: symbol, name: CreateEntryDialog }
            claims:
              - kind: satisfies
                criterion: REQ-FE-043#criterion.readable-errors
              - kind: satisfies
                criterion: REQ-FE-044#criterion.dictionary-localization
          - id: search-bar
            adapter: typescript
            path: frontend/src/components/SearchBar.tsx
            selector: { kind: symbol, name: SearchBar }
            claims:
              - kind: satisfies
                criterion: REQ-FE-044#criterion.dictionary-localization
              - kind: satisfies
                criterion: REQ-FE-014#criterion.search-input
              - kind: satisfies
                criterion: REQ-FE-014#criterion.keyboard-shortcut
          - id: locale-dictionary
            adapter: typescript
            path: frontend/src/lib/i18n.ts
            selector: { kind: symbol, name: t }
            claims:
              - kind: satisfies
                criterion: REQ-FE-044#criterion.dictionary-localization
  - id: FEAT-FE-005
    title: Form grid interaction surfaces
    summary: Connect the Form grid's search, sorting, filtering, CSV export, Add Row flow, and inline editing behavior to exact frontend verification evidence.
    status: implemented
    bindings:
      - id: frontend
        role: implementation
        facet: frontend
        responsibility: Keep Form grid interaction behavior connected to the shipped FormTable and search store implementation.
        targets:
          - id: form-table
            adapter: typescript
            path: frontend/src/components/FormTable.tsx
            selector: { kind: symbol, name: FormTable }
            claims:
              - kind: satisfies
                criterion: REQ-FE-019#criterion.multi-column-sort
              - kind: satisfies
                criterion: REQ-FE-020#criterion.hybrid-filtering
              - kind: satisfies
                criterion: REQ-FE-021#criterion.context-aware-export
              - kind: satisfies
                criterion: REQ-FE-030#criterion.form-aware-add-row
              - kind: satisfies
                criterion: REQ-FE-031#criterion.inline-edit-selection
          - id: search-store
            adapter: typescript
            path: frontend/src/lib/search-store.ts
            selector: { kind: symbol, name: createSearchStore }
            claims:
              - kind: satisfies
                criterion: REQ-FE-020#criterion.hybrid-filtering

  - id: FEAT-FE-006
    title: SQL editor and query session clients
    summary: Connect the browser SQL editor, shared SQL rules, saved-query client, and SQL-session client to exact frontend verification evidence.
    status: implemented
    bindings:
      - id: frontend
        role: implementation
        facet: frontend
        responsibility: Keep SQL editing, shared lint and completion rules, saved-query lifecycle calls, and SQL-session result access connected to the shipped frontend artifacts.
        targets:
          - id: sql-editor
            adapter: typescript
            path: frontend/src/components/SqlQueryEditor.tsx
            selector: { kind: symbol, name: SqlQueryEditor }
            claims:
              - kind: satisfies
                criterion: REQ-FE-036#criterion.editor-contract
              - kind: satisfies
                criterion: REQ-FE-036#criterion.schema-completion
          - id: sql-helpers
            adapter: typescript
            path: frontend/src/lib/sql.ts
            selector: { kind: symbol, name: sqlLintDiagnostics }
            claims:
              - kind: satisfies
                criterion: REQ-FE-036#criterion.editor-contract
              - kind: satisfies
                criterion: REQ-FE-036#criterion.shared-rules
          - id: shared-sql-rules
            adapter: declared
            path: shared/sql/ugoite-sql-rules.json
            selector: { kind: file }
            claims:
              - kind: satisfies
                criterion: REQ-FE-036#criterion.shared-rules
          - id: saved-sql-client
            adapter: typescript
            path: frontend/src/lib/sql-api.ts
            selector: { kind: symbol, name: sqlApi }
            claims:
              - kind: satisfies
                criterion: REQ-FE-050#criterion.saved-query-lifecycle
              - kind: satisfies
                criterion: REQ-FE-050#criterion.typed-errors
          - id: sql-session-client
            adapter: typescript
            path: frontend/src/lib/sql-session-api.ts
            selector: { kind: symbol, name: sqlSessionApi }
            claims:
              - kind: satisfies
                criterion: REQ-FE-051#criterion.session-lifecycle
              - kind: satisfies
                criterion: REQ-FE-051#criterion.typed-errors
