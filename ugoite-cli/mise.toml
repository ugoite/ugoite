[env]
CARGO_TARGET_DIR = "../target/rust"

[tasks.install]
run = "cargo clean -p ugoite-cli && cargo build"
description = "Build ugoite-cli"

[tasks.test]
run = "cargo clean -p ugoite-cli && cargo test"
description = "Run ugoite-cli Rust test suite"
