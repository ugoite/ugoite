# Changesets Policy

This repository uses Changesets to keep monorepo change intent explicit.

- Add one `.changeset/*.md` file for releasable behavior changes.
- Use Conventional Commits for commit messages.
- Semantic-release remains the automated publisher for Git tags and GitHub releases.
