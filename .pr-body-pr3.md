## Summary

- Remove Entry-level title semantics from the domain, storage, query, REST, MCP, and frontend surfaces.
- Keep unclaimed physical columns readable by their physical names without migration or inferred meaning.
- Remove title-specific Markdown conversion, search, ordering, SQL projection, and compatibility tests while preserving structured fields, history, and Space 0.1 reads.

## Related Issue (required)

Closes #2956

## Testing

- `cargo check --workspace --all-targets --locked`
- `cargo test -p ugoite-core --lib --locked`
- `cargo test -p ugoite-iceberg --test test_entry --test test_saved_sql --test test_space_version_compat --test test_knowledge_compatibility --locked`
- `cargo test -p ugoite-server --lib --locked`
- `deno run -A npm:vitest run --maxWorkers=1 --no-file-parallelism` (from `frontend/`)
- `deno task check`
- `./scripts/mitase check .`

## Knowledge Compatibility Review

- [x] Breaking semantic change; an explicit versioned contract or migration/re-encoding decision is documented.

Existing Space 0.1 knowledge remains readable without migration. The product/interface cleanup removes Entry-level title interpretation only; user-defined Form fields and append-only revision/history semantics remain intact.
Decision: This is the planned v0.2 product/interface breaking boundary. Existing Space 0.1 bytes remain readable; only the removed Entry-level title interpretation and related public aliases are no longer part of the product contract.
